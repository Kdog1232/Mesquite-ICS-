type StatusValue = 'IN' | 'OUT';
export function StatusToggle({name,status,disabled,onChange}:{name:string;status:StatusValue;disabled:boolean;onChange:()=>void}){const out=status==='OUT';return <div className="flex items-center gap-3"><span className={!out?'font-black text-emerald-800':'font-semibold text-slate-500'}>IN</span><button type="button" role="switch" aria-checked={out} aria-label={`Mark ${name} ${out?'in':'out'}`} disabled={disabled} onClick={onChange} className={`relative h-12 w-24 rounded-full border-2 transition disabled:cursor-wait disabled:opacity-60 ${out?'border-red-700 bg-red-700':'border-emerald-700 bg-emerald-700'}`}><span className={`absolute top-1 h-9 w-9 rounded-full bg-white shadow transition-transform ${out?'left-1 translate-x-12':'left-1'}`}/></button><span className={out?'font-black text-red-800':'font-semibold text-slate-500'}>OUT</span></div>}
